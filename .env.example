# Calm Cockpit Environment Variables

# The secret key for securely signing the session cookie.
# Run `python -c "import secrets; print(secrets.token_hex())"` to generate a strong key.
SECRET_KEY=your-super-secret-key-goes-here
